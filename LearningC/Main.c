# include <stdio.h>
# include <ctype.h>
# include <string.h>


main()
{
    int iDate = getchar();
    if (isdigit(iDate))
    {

    }
    else

    {
        printf("% is not a date.\n",iDate);
    }

}

bool DateCheck( iDate)
{
    bool isDate;



    return isDate;
}